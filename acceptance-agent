#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
VENV_PY="$ROOT_DIR/apps/api/.venv/bin/python"
CLI_PY="$ROOT_DIR/cli/main.py"

if [[ -x "$VENV_PY" ]]; then
  exec "$VENV_PY" "$CLI_PY" "$@"
fi

exec python3 "$CLI_PY" "$@"
